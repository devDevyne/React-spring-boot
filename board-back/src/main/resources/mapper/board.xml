<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devyne.board.boardback.Mapper.BoardMapper">

    <select id="selBoardList" resultType="BoardVO">
        SELECT BOARD_IDX, TITLE, AUTHOR, REG_DATE, MOD_DATE FROM BOARD
    </select>

    <insert id="insBoard" parameterType="BoardVO">
        INSERT INTO 
        BOARD (TITLE, AUTHOR, REG_DATE, MOD_DATE, CONTENTS)
        VALUES (#{title}, #{author}, now(), now(), #{contents})
    </insert>
    
</mapper>